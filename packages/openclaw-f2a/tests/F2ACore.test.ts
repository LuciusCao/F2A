import { describe, it, expect, vi } from 'vitest';

describe('F2ACore', () => {
  describe('event registration', () => {
    it('should register peer:message event (not message:received)', () => {
      // TODO: 测试事件名是否正确
      expect(true).toBe(true);
    });
  });
  
  describe('onMessageCallback', () => {
    it('should be defined when registering event listener', () => {
      // TODO: 测试回调是否存在
      expect(true).toBe(true);
    });
  });
});
