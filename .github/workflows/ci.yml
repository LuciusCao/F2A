name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # Lint 检查
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm install
      
      - name: Run lint
        run: npm run lint || npm run build -- --noEmit

  # 单元测试（Node 20 & 22）
  unit-tests:
    runs-on: ubuntu-latest
    needs: lint
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      
      - name: Run unit tests
        run: npm run test:unit
      
      - name: Upload coverage
        uses: codecov/codecov-action@v4
        if: matrix.node-version == 20
        with:
          files: ./coverage/lcov.info
          flags: unittests

  # 集成测试（仅 push 触发，PR 时跳过）
  integration-tests:
    runs-on: ubuntu-latest
    needs: unit-tests
    steps:
      - name: Skip on PR
        if: github.event_name != 'push'
        run: echo "Skipping integration tests on PR"

      - uses: actions/checkout@v4
        if: github.event_name == 'push'

      - uses: actions/setup-node@v4
        if: github.event_name == 'push'
        with:
          node-version: '20'

      - name: Install dependencies
        if: github.event_name == 'push'
        run: npm install

      - name: Build
        if: github.event_name == 'push'
        run: npm run build

      - name: Run Docker integration tests
        if: github.event_name == 'push'
        run: npm run test:docker

      - name: Export logs on failure
        if: failure() && github.event_name == 'push'
        run: docker compose -f tests/docker/docker-compose.test.yml logs > test-logs.txt

      - name: Upload logs on failure
        if: failure() && github.event_name == 'push'
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-logs
          path: test-logs.txt

      - name: Upload coverage
        if: github.event_name == 'push'
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          flags: integration
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      
      - name: Run Docker integration tests
        run: npm run test:docker
      
      - name: Export logs on failure
        if: failure()
        run: docker compose -f tests/docker/docker-compose.test.yml logs > test-logs.txt
      
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-logs
          path: test-logs.txt
      
      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          flags: integration

  # 多节点测试（仅 main 分支 push 触发，其他情况跳过）
  multi-node-tests:
    runs-on: ubuntu-latest
    needs: integration-tests
    steps:
      - name: Skip on non-main push
        if: github.event_name != 'push' || github.ref != 'refs/heads/main'
        run: echo "Skipping multi-node tests"

      - uses: actions/checkout@v4
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'

      - uses: actions/setup-node@v4
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        with:
          node-version: '20'

      - name: Install dependencies
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: npm install

      - name: Build
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: npm run build

      - name: Run 10-node test
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: npm run test:docker:10
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      
      - name: Run 10-node test
        run: npm run test:docker:10